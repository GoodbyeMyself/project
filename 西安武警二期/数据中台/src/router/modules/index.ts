import { AppRouteRecord } from '@/types/router'
import { dataMiddlePlatformRoutes } from './data-middle-platform'

/**
 * 导出所有模块化路由
 */
export const routeModules: AppRouteRecord[] = [
  dataMiddlePlatformRoutes
]
