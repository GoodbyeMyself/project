import { AppRouteRecord } from '@/types/router'
import { indicatorPlatformRoutes } from './indicator-platform'

/**
 * 导出所有模块化路由
 */
export const routeModules: AppRouteRecord[] = [
  indicatorPlatformRoutes
]
